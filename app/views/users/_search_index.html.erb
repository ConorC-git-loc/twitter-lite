<div class="container" style="min-height:594px" %>
  <br />
  <strong>Search the community</strong>
  <br>
  <br>
  <%= search_form_for @q, url: tweets_path do |f| %>
  <div class="form-group">
    <%= f.search_field :username_cont, placeholder: 'Name', class: "form-control" %>
    </div>
    <div class="form-group">
    <%= f.search_field :email_cont, placeholder: 'Email', class: "form-control" %>
    </div>
    <div class="form-group">
    <%= f.search_field :handle_cont, placeholder: '@', class: "form-control" %>
    </div>
    <%= f.submit "Search", class: "btn btn-primary" %>
  <% end %>
  <br>


  <table id="users" class="ml-2">
  <thead>
	<tr>
		<th style="padding:0 15px 0 15px;">Name</th>
		<th style="padding:0 15px 0 15px;">About</th>
		<th style="padding:0 15px 0 15px;">@</th>
		<th colspan="3"></th>
		<th colspan="3"></th>
	</tr>
	</thead>
	<tbody>
	<% @users.each do |user| %>
	<tr>
		<td style="padding:0 15px 0 15px;"><%= user.username %></td>
		<td style="padding:0 15px 0 15px;"><%= user.about %></td>
		<td style="padding:0 15px 0 15px;"><%= user.handle %></td>
    <td style="padding:0 15px 0 15px;"><%= link_to "Profile", user_path(user.id), class: "btn btn-outline-primary rounded-pill" %> </td>

		<!--<td><% if current_user.id != user.id %> 
             <% if current_user.following?(user) %>
               <%= button_to "Unfollow", relationships_path(user_id: user.id), :class => "follow-sm-btn btn btn-primary rounded-pill" %>
             <% else %>
               <%= button_to "Follow", relationships_path(user_id: user.id), :class => "follow-sm-btn btn btn-primary rounded-pill" %>
            <% end %>
           <% end %></td>
        <td stlye="padding:0 60px 0 60px;" col width="130" align="right"><b><%= user.followers.count %> Followers</b></td>
	</tr>-->
	<% end %>
	</tbody>
  <%= will_paginate @users, renderer: WillPaginate::ActionView::BootstrapLinkRenderer, class: "fixed-bottom" %>
  </table>
</div>